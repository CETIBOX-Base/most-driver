diff --git a/aim-network/networking.c b/aim-network/networking.c
index 0607022..4639c49 100644
--- a/aim-network/networking.c
+++ b/aim-network/networking.c
@@ -349,7 +349,7 @@ static int aim_probe_channel(struct most_interface *iface, int channel_idx,
 
 	if (nd->tx.linked || nd->rx.linked) {
 		struct net_device *dev =
-			alloc_netdev(0, "meth%d", most_nd_setup);
+			alloc_netdev(0, "meth%d", NET_NAME_UNKNOWN, most_nd_setup);
 
 		if (!dev) {
 			pr_err("no memory for net_device\n");
diff --git a/aim-sound/sound.c b/aim-sound/sound.c
index e989a74..860302e 100644
--- a/aim-sound/sound.c
+++ b/aim-sound/sound.c
@@ -591,8 +591,8 @@ static int audio_probe_channel(struct most_interface *iface, int channel_id,
 	if (audio_set_pcm_format(pcm_format, cfg))
 		return ret;
 
-	ret = snd_card_create(-1, card_name, THIS_MODULE,
-			      sizeof(*channel), &card);
+	ret = snd_card_new(NULL, -1, card_name, THIS_MODULE,
+			   sizeof(*channel), &card);
 	if (ret < 0)
 		return ret;
 
